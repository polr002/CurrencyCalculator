import EStlyleSheet from 'react-native-extended-stylesheet';

export default EStlyleSheet.create({
	container: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
		backgroundColor: '$primaryBlue',
	},
});